email = (unique=True, nullable=True) CANT LEAVE BLANK MORE THAN ONE TIMES

nullable works


unique works but cant leave blank more than one time even if nullable is true
    fullname = db.Column(db.String(50),     unique=False,          nullable=True)
    email = db.Column(db.Text(50),          unique=True,           nullable=True)
    username = db.Column(db.String(20),     unique=True,           nullable=False)
    phone_number = db.Column(db.Integer,    unique=True,           nullable=False)
    password = db.Column(db.Text(),         unique=True,           nullable=False)



        if emails and password == login1.password and login1.email:
            # print(login1,password,"and",login1.password)
            print(emails,password,"and",login1.password,login1.email)
            login_user(login1)
            print("successfully login")



heroku and githun except name
password - 9438mna@
name lohitkrishnanaik
email lohitkrishnanaik2@gmail.com



git commit -m"Initial commit"

https://todo-todowebsite.herokuapp.com/ - my website location


stac oveflow
name lucky
email 9438mna@